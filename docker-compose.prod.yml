version: '3.8'

services:
  loan-backend:
    image: loan-backend
    secrets:
      - openai_api_key
    environment:
      - AI_API_KEY_FILE=/run/secrets/openai_api_key
      # Other env vars...

secrets:
  openai_api_key:
    file: ./secrets/openai_api_key.txt  # In production, use external secrets management
